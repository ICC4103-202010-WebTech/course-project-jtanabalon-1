<%= form_with(model: file_event, local: true) do |form| %>
  <% if file_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(file_event.errors.count, "error") %> prohibited this file_event from being saved:</h2>

      <ul>
        <% file_event.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="field">
    <%= form.label :event_id_id %>
    <%= form.text_field :event_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
