<%= form_with(model: video_event, local: true) do |form| %>
  <% if video_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video_event.errors.count, "error") %> prohibited this video_event from being saved:</h2>

      <ul>
        <% video_event.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="field">
    <%= form.label :event_id_id %>
    <%= form.text_field :event_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
